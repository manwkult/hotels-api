# Hotels API

> Node.js & Express

## API
- **List Hotels**
  - `/api/hotels/list.do?filters={}` --> GET

## Build Setup

``` bash
# install dependencies
$ npm install

# serve with hot reload (nodemon) at localhost:5000
$ npm run dev
```